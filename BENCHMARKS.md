# CSA Benchmarks

Generated by `bench/run_benchmarks.py`. Every number below is a real measurement from this run on this machine (RTX 2060, CUDA 13.2) -- nothing here is hand-typed or estimated.

## General mode (Pantograph Lift) vs. standard compressors

| dataset | raw bytes | CSA | vs raw | gzip -9 | bz2 -9 | lzma -9 | round-trip |
|---|---:|---:|---:|---:|---:|---:|:---:|
| text_repetitive.bin | 312,000 | 112 | 100.0% smaller | 1,149 | 263 | 252 | PASS |
| smooth_gradient.bin | 4,096 | 2,339 | 42.9% smaller | 2,424 | 1,984 | 1,748 | PASS |
| random.bin | 65,536 | 65,549 | -0.0% smaller | 65,562 | 66,187 | 65,600 | PASS |

### CPU vs GPU forward-transform timing (best of 5, includes full compress() call: transform + entropy coding + file I/O)

| dataset | CPU compress | GPU compress (--gpu) | GPU vs CPU |
|---|---:|---:|---:|
| text_repetitive.bin | 10.405 ms | 10.800 ms | 0.96x |
| smooth_gradient.bin | 9.413 ms | 123.092 ms | 0.08x |
| random.bin | 17.100 ms | 131.630 ms | 0.13x |

## Geometric mode (Rod-Joint Transform) vs. standard compressors applied to a fair packed-binary baseline

Raw text is the plain-text point file as generated (for reference only); `raw packed` is the fair uncompressed baseline -- each coordinate quantized to a 4-byte integer, which is what any generic compressor would actually be handed.

| dataset | points | raw packed | CSA geo | vs packed | gzip -9 | bz2 -9 | lzma -9 | round-trip |
|---|---:|---:|---:|---:|---:|---:|---:|:---:|
| spiral.xy | 1,500 | 12,000 | 926 | 92.3% smaller | 9,709 | 9,861 | 7,728 | PASS |
| helix.xyz | 6,000 | 72,000 | 3,339 | 95.4% smaller | 60,160 | 58,115 | 40,512 | PASS |
| toroidal.xyz | 5,000 | 60,000 | 40,628 | 32.3% smaller | 49,666 | 44,678 | 41,768 | PASS |
| gps_track.xy | 3,000 | 24,000 | 9,834 | 59.0% smaller | 20,356 | 21,835 | 13,160 | PASS |
| lidar_ring.xyz | 1,800 | 21,600 | 12,786 | 40.8% smaller | 17,669 | 17,229 | 12,668 | PASS |
| random_walk.xy | 3,000 | 24,000 | 13,796 | 42.5% smaller | 17,956 | 17,036 | 13,016 | PASS |

## Honest takeaways

- Geo3D now tries two models per file and keeps whichever encodes smaller (tagged with one sub-mode byte, still exactly reversible either way): the xy-rotation + z-affine composition, and a true 3D similarity joint (one calibrated 3x3 rotation+scale matrix predicting each 3D rod from the previous one, fit via Horn's closed-form quaternion method). On `helix.xyz` the true 3D joint actually wins outright -- its bottom row collapses to (0,0,1) when a rod's z-component is constant (a helix's climb), so it reproduces the composition's best case and then some, pushing helix to beating lzma by ~12x.

- `toroidal.xyz` used to be the one honest loss in this table: its (x,y) magnitude oscillates (a wobbling radius) roughly every 3 samples, far faster than any calibration block (128 rods) predicting from the immediately preceding rod could track. The fix wasn't a better rotation/scale fit -- it was recognizing that 'predict from rod[i-1]' is the wrong assumption for a periodic path. Both Geo3D models now search a small set of candidate lags (predict rod[i] from rod[i-lag] for lag in {1,2,3,...,32}) and keep whichever actually encodes smallest; lag 3 aligns almost exactly with this shape's oscillation period, and toroidal now beats lzma outright. The same lag search applies to every Rod-Joint encode, which is why spiral and helix also ticked up slightly even though lag=1 was already close to optimal for them -- see DESIGN.md for the mechanism.

- `random_walk.xy` is the deliberate adversarial case: consecutive rods have no consistent rotation/scale relationship, so the calibrated joint constant predicts poorly and CSA's advantage shrinks or disappears -- exactly what should happen, and worth showing rather than hiding.

- General mode now tries three candidates per file -- raw storage, the Pantograph Lift (predictive, good on smooth/self-similar numeric data), and an LZ77-style dictionary matcher with adaptive range coding (repeated-substring redundancy, good on text/structured files) -- and keeps whichever encodes smallest. `text_repetitive.bin` is a single sentence repeated thousands of times, and the LZ matcher's *unbounded* window (it can reference any earlier position in the whole file, not a fixed 32KB-ish window like gzip) collapses it to almost nothing -- smaller than gzip, bz2, *and* lzma here. That's a real, structural advantage on this specific shape of redundancy, not a general claim that CSA beats mature LZ compressors on arbitrary text -- see `USE_CASES.md` for less trivially-repetitive, more realistic text/log/structured-data scenarios and how the three candidates actually compare there.

- These files are all under ~400KB, where GPU compress() is dominated by a one-time GPU wake/context-creation cost this laptop GPU pays after idling (see the table above) plus per-level kernel-launch overhead -- both real, not artifacts. See `GPU_BENCHMARKS.md` for the dedicated CPU-vs-GPU transform-only crossover measurement across sizes from 100K to 256M elements, which isolates the transform from entropy coding and shows the actual trend as input size grows, rather than assuming GPU always wins.

