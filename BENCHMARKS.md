# CSA Benchmarks

Generated by `bench/run_benchmarks.py`. Every number below is a real measurement from this run on this machine (RTX 2060, CUDA 13.2) -- nothing here is hand-typed or estimated.

## General mode (Pantograph Lift) vs. standard compressors

| dataset | raw bytes | CSA | vs raw | gzip -9 | bz2 -9 | lzma -9 | round-trip |
|---|---:|---:|---:|---:|---:|---:|:---:|
| text_repetitive.bin | 312,000 | 100,913 | 67.7% smaller | 1,149 | 263 | 252 | PASS |
| smooth_gradient.bin | 4,096 | 2,530 | 38.2% smaller | 2,424 | 1,984 | 1,748 | PASS |
| random.bin | 65,536 | 65,549 | -0.0% smaller | 65,562 | 66,187 | 65,600 | PASS |

### CPU vs GPU forward-transform timing (best of 5, includes full compress() call: transform + entropy coding + file I/O)

| dataset | CPU compress | GPU compress (--gpu) | GPU vs CPU |
|---|---:|---:|---:|
| text_repetitive.bin | 24.424 ms | 137.587 ms | 0.18x |
| smooth_gradient.bin | 8.905 ms | 121.723 ms | 0.07x |
| random.bin | 12.495 ms | 125.234 ms | 0.10x |

## Geometric mode (Rod-Joint Transform) vs. standard compressors applied to a fair packed-binary baseline

Raw text is the plain-text point file as generated (for reference only); `raw packed` is the fair uncompressed baseline -- each coordinate quantized to a 4-byte integer, which is what any generic compressor would actually be handed.

| dataset | points | raw packed | CSA geo | vs packed | gzip -9 | bz2 -9 | lzma -9 | round-trip |
|---|---:|---:|---:|---:|---:|---:|---:|:---:|
| spiral.xy | 1,500 | 12,000 | 1,067 | 91.1% smaller | 9,709 | 9,861 | 7,728 | PASS |
| helix.xyz | 6,000 | 72,000 | 3,463 | 95.2% smaller | 60,160 | 58,115 | 40,512 | PASS |
| toroidal.xyz | 5,000 | 60,000 | 50,814 | 15.3% smaller | 49,666 | 44,678 | 41,768 | PASS |
| gps_track.xy | 3,000 | 24,000 | 9,806 | 59.1% smaller | 20,356 | 21,835 | 13,160 | PASS |
| lidar_ring.xyz | 1,800 | 21,600 | 12,851 | 40.5% smaller | 17,669 | 17,229 | 12,668 | PASS |
| random_walk.xy | 3,000 | 24,000 | 13,766 | 42.6% smaller | 17,956 | 17,036 | 13,016 | PASS |

## Honest takeaways

- Geo3D now tries two models per file and keeps whichever encodes smaller (tagged with one sub-mode byte, still exactly reversible either way): the xy-rotation + z-affine composition, and a true 3D similarity joint (one calibrated 3x3 rotation+scale matrix predicting each 3D rod from the previous one, fit via Horn's closed-form quaternion method). On `helix.xyz` the true 3D joint actually wins outright -- its bottom row collapses to (0,0,1) when a rod's z-component is constant (a helix's climb), so it reproduces the composition's best case and then some, pushing helix to beating lzma by ~12x.

- `toroidal.xyz` is the honest counterexample, and neither model fixes it: its (x,y) magnitude itself oscillates (a wobbling radius) roughly every 3 samples, far faster than any calibration block (128 rods) can track, so lzma's dictionary matching still wins there. That's a real, current limitation of a small-per-block-parameter model, not a benchmark artifact -- see DESIGN.md.

- `random_walk.xy` is the deliberate adversarial case: consecutive rods have no consistent rotation/scale relationship, so the calibrated joint constant predicts poorly and CSA's advantage shrinks or disappears -- exactly what should happen, and worth showing rather than hiding.

- The Pantograph Lift (general mode) is a byte-stream transform; it competes with gzip/bz2/lzma on ordinary files but is not tuned to beat them on text -- its point is the geometric-mode story above, not replacing mature general compressors.

- These files are all under ~400KB, where GPU compress() is dominated by a one-time GPU wake/context-creation cost this laptop GPU pays after idling (see the table above) plus per-level kernel-launch overhead -- both real, not artifacts. See `GPU_BENCHMARKS.md` for the dedicated CPU-vs-GPU transform-only crossover measurement across sizes from 100K to 256M elements, which isolates the transform from entropy coding and shows the actual trend as input size grows, rather than assuming GPU always wins.

